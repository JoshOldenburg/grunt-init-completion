`grunt-init-completion`
=======================
A supplement to grunt-init to add bash completion.

To install:
```sh
npm install --global grunt-init-completion
grunt-init-completion > ~/.bash_profile # .bashrc for Linux, or .zshrc
```
